# Kontext V2 Benchmark Matrix

Last updated: 2026-05-22

## Sources

- Mem0 memory-benchmarks repository: https://github.com/mem0ai/memory-benchmarks
- Inspected upstream commit: `4b61c5d31b9c668a12b4f5e78064248a02c82d2b`
- Mem0 memory evaluation docs: https://docs.mem0.ai/core-concepts/memory-evaluation
- OB1 base eval: `tools/mem0-remote-mcp/retrieval_eval_cases.v1.13.json`
- OB1 expanded eval: `tools/mem0-remote-mcp/retrieval_eval_cases.v1.14-expanded.json`

## Current Gate Matrix

| Gate | Source / fixture | Pass criteria | Mem0 latest | Kontext latest | Gap / next action |
| --- | --- | --- | --- | --- | --- |
| OB1 base live MCP retrieval | 12-case V1.13 eval | Domain/type hit and minimum results for every case | 12/12, avg 872.68 ms; first-satisfying MRR 0.7389 | 12/12, avg 715.9 ms; first-satisfying MRR 1.0 | Kontext equal on pass rate, faster, and better on first-satisfying rank quality in fresh shadow report `20260521T131811Z`; overlap_any 0.75, overlap_first 0.25, mean first-overlap MRR 0.6285, with 3 no-overlap cases. The first-satisfying metric confirms the no-overlap cases are mostly quality-vs-exact-ID differences, not correctness losses. |
| OB1 expanded live MCP retrieval | 36-case V1.14 expanded eval | Domain/type hit and minimum results for every case | 35/36, avg 945.84 ms; first-satisfying MRR 0.8565 | 36/36, avg 741.45 ms; first-satisfying MRR 1.0 | Kontext is better on pass rate, faster, and better on first-satisfying rank quality in the fresh 2026-05-21 Codex-profile rerun after the sensitive-agent-context fix. The previous only quality-rank loss, `psychology-current-rules`, is now rank 1 in Kontext and rank 2 in Mem0. Kontext has first satisfying hits `36/36`; it beats Mem0 on 8 cases, loses on 0, and ties on 28. Exact overlap remains separate: overlap_any 0.7222, overlap_first 0.25, mean first-overlap MRR 0.5792, no-overlap 10. Mem0's one failed expanded case in this run was `psychology-execution-patterns`. |
| Mirror sync and exact-ID freshness | VPS shadow report, `sync_cli --refresh-existing-limit/--refresh-existing-offset`, `--refresh-id-file`, and `exact_id_freshness_cli --limit/--offset` | Bounded sync must refresh existing mirrored Mem0 IDs outside the public 20-row snapshot; exact checked IDs must be fresh without printing raw memory text; scheduled checks must advance across the mirror over time; eval-returned missing Mem0 IDs can be exact-fetched safely | Mem0 remains the source of truth | Rolling exact windows are deployed and continue passing. Eval-assisted exact-ID backfill fetched 23 missing Mem0 top IDs from the expanded no-overlap diagnostics and applied them to the mirror after a DB backup. Fresh shadow report `20260521T131811Z` is `ok=true`: exact freshness checked offset 2500, `250/250` fresh, next offset 2750, mirror rows 4003, and base live MCP reliability stayed Kontext 12/12 and Mem0 12/12. | Previous freshness and missing-ID gaps are closed for the checked windows and benchmark-relevant no-overlap IDs. Next: continue to another benchmark/cutover gate; avoid optimizing for exact ID overlap when first-satisfying rank proves Kontext is better. |
| Mem0-compatible MCP write surface | Kontext canary profile + isolated DB smoke | Direct tools available; live canary dry-runs; isolated write-enabled app applies safely; real Codex/Claude client configs point at remote MCP | Live Mem0 writer tools are production source of truth | Public canary route initializes and lists 16 tools; reusable canary smoke verifies `search`, `fetch`, `ingestion_status`, project continuity tools, category tools, `save`, `update`, `delete`, `flag_memory`, `extract_memories`, `ingest_exchange`, and `submit_memory_override`. Memory write-like calls return `mode=dry_run`, `writes_applied=0`, and `delete` returns `delete_candidate`; project continuity search/fetch/timeline/file-context and categories/list-category-memory checks pass after seeding a compact project observation. Local Codex lists `mem0_kontext_canary`; local Claude `mcp get mem0_kontext_canary` reports connected. Fresh noninteractive Codex native probe passed with Mem0 temp canary create/delete and Kontext dry-run shadow checks. The repeatable safe shape is now captured in `tools/kontext-v2/scripts/codex_native_mcp_canary.py` and uses `codex -a never exec --sandbox danger-full-access --ephemeral -C <repo> -o <out> -`; older read-only `codex exec` probes can cancel MCP calls. Native Claude MCP probe passed Mem0 temp create/delete and Kontext dry-run checks; it initially exposed an oversized category response, so MCP category rows were compacted and deployed. Follow-up live smoke and Claude category probe now pass with `category_count=6`, `memory_count=3`, and `output_too_large=false`; the smoke now enforces payload-size budgets and reports `list_categories=2811` bytes / `list_category_memories=4705` bytes. Isolated schema `save/update/flag/delete` apply, `extract_memories` remains dry-run. Kontext now supports profile-scoped write allowlists via `KONTEXT_MCP_WRITE_PROFILES` / `KONTEXT_MCP_WRITE_ENABLED_PROFILES` and separately gates category mutators with `KONTEXT_MCP_CATEGORY_WRITE_PROFILES`. A temporary isolated-schema canary server with only the `mem0` compatibility profile write-enabled verified `save`, `update`, `delete`, `flag_memory`, and `submit_memory_override` in `apply` mode with one write each while `extract_memories`/`ingest_exchange` stayed dry-run; category mutators were hidden, and the temp schema/container were dropped. Deployed health remains `mode=mirror_read_only`, `writes.enabled=false`, and live canary smoke still reports all writes as dry-run with `writes_applied=0`. | Next: run the reusable native Codex canary after reconnects when client behavior needs proof, or run a public-route canary write activation with rollback only after explicit approval; do not enable production writes yet. |
| State/freshness/decay behavior | Local and VPS sanitized state behavior eval, `tools/kontext-v2/scripts/kontext_state_behavior_eval.py` | Synthetic rows only; current active facts must beat stale/superseded conflicts; cold tier must be demoted unless explicitly requested; destructive delete/update intents must become dry-run `delete_candidate` / `conflict_candidate` flags; score explanations must not leak memory text | Mem0 remains source of truth; this gate checks Kontext safety behavior against the agent-first write policy | Local TDD gate passes `7/7` with `writes_applied=0`; focused pytest passes `2 passed`; script smoke prints `kontext-state-behavior ok=true passed=7/7 writes_applied=0`. VPS report `/opt/kontext/reports/kontext-state-behavior-20260521T150041Z.json` is `ok=true`, `passed=7/7`, and `writes_applied=0`. | Gate closed for local and VPS shadow mode. Keep it as formal cutover-review evidence alongside MCP canary and exact-ID freshness. |
| Predict-only reporting validity | Shared benchmark reporting for LoCoMo, LongMemEval, and BEAM | Questions without reachable evidence IDs or expected terms are skipped from retrieval-evaluable pass rates and counted as `skipped_no_match_rule` | Not applicable; this is Kontext's local mirror harness correctness gate | Deployed. Local reporting regression passes; remote isolated BEAM-shaped smoke reports `retrieval_evaluable_questions=1`, `skipped_no_match_rule=1`, and top-k retrieval rate `1.0` without raw fixture output. LoCoMo now filters unreachable evidence IDs before retrieval-evaluable scoring. | Use retrieval-evaluable rates for predict-only gates; keep `matched_questions/total_questions` as legacy headline only when all questions have a match rule. |
| Official LoCoMo predict-only mirror | Mem0 `memory-benchmarks` LoCoMo shape, local Kontext adapter | Isolated benchmark data; sanitized reports; evidence hit analysis | Published Mem0 Platform: 92.5% top-200, 91.8% top-50 | Full public LoCoMo predict-only sweep deployed and rerun on VPS. After filtering unreachable evidence IDs from the retrieval-evaluable set: top-k 10 `1835/1977` (92.8%), top-k 20 `1942/1977` (98.2%), top-k 50 `1977/1977` (100.0%), top-k 200 `1977/1977` (100.0%), with 9 skipped no-match-rule/unreachable-evidence rows and no top-k 50 misses. | Kontext exceeds Mem0 published LoCoMo top-50/top-200 retrieval targets on evidence-hit rate. This remains predict-only retrieval evidence, not judged answer accuracy. |
| Official LongMemEval predict-only mirror | Mem0 `memory-benchmarks` LongMemEval shape | Isolated per-question user namespace; sanitized predict-only reports; top-k sweep | Published Mem0 Platform judged answer score: 94.4% top-200, 94.8% top-50 | Implemented and deployed to VPS. Capped type sweep: 60/60 at top-50 and top-200. Tuned full public predict-only retrieval run: top-10 467/500 (93.4%), top-20 484/500 (96.8%), top-50 500/500 (100.0%), top-200 500/500 (100.0%), avg search latency 1698.6 ms. | Kontext exceeds Mem0 published LongMemEval top-50/top-200 rates on retrieval evidence-hit rate, but this is not yet the same as judged answer accuracy. The five previous top-50 misses now land at ranks 32-41 after benchmark-only session-context ranking. Next: design the first safe BEAM slice. |
| Official BEAM predict-only mirror | Mem0 `memory-benchmarks` BEAM shape plus Hugging Face datasets-server rows slice | Isolated benchmark data; sanitized reports; evidence-backed question slice; top-k sweep | Published Mem0 Platform judged answer score: BEAM 1M 70.1% top-200; BEAM 10M 50.5% top-200 | Implemented and deployed to VPS. Local fixture normalization passes 4/4, including zero-valued `source_chat_ids`. Remote isolated-schema BEAM 100K smoke: 4/4 evidence-backed questions at top-k 10/20/50/200. Remote isolated-schema BEAM 1M 3-row slice after zero-id fix: 60/60 total questions, including 53 evidence-backed questions, at top-k 10/20/50/200; avg search latency 1969.0 ms; schema dropped. Larger capped slices after reporting fix: BEAM 100K offset 0 first 5 rows cap 80 scored 72/72 retrieval-evaluable at top-k 10/20/50/200 with 8 skipped rows, avg 256.0 ms; BEAM 1M offset 0 first 5 rows cap 80 scored 71/71 retrieval-evaluable with 9 skipped rows, avg 2003.6 ms; BEAM 100K offset 5 first 5 rows cap 80 scored 71/71 retrieval-evaluable with 9 skipped rows, avg 321.6 ms; BEAM 1M offset 5 first 5 rows cap 80 scored 71/71 retrieval-evaluable with 9 skipped rows, avg 2440.8 ms. BEAM 10M offset 0 first row cap 20 initially scored 12/18 retrieval-evaluable at top-k 200 because the benchmark adapter loaded only 10k of ~19.9k candidates; after raising the benchmark candidate limit to 50k, the same smoke scored top-k 10/20 15/18, top-k 50 17/18, and top-k 200 18/18, with 2 skipped rows and avg 26770.0 ms. After deploying benchmark score-text caching plus benchmark-only tail reranking, BEAM 10M offsets 0-5 with cap 20 per row score top-k 200 `105/105` retrieval-evaluable, top-k 50 `102/105`, top-k 20 `89/105`, and top-k 10 `82/105`, with 15 skipped no-match-rule rows. The offset 3-5 slice rerun preserved top-k 10 `40/51` and top-k 20 `44/51`, improved top-k 50 from `49/51` to `51/51`, and kept top-k 200 `51/51`; no top-k 50 misses remain on that slice. An opt-in benchmark-only semantic tail reranker is now deployed for BEAM diagnostics; targeted offset 9 instruction/preference smoke improves top-k 50 from `3/4` to `4/4` and keeps top-k 200 `4/4`. Health remains mirror_read_only/writes disabled. | Next: test semantic rerank against the earlier offset 6-8 misses and decide whether it should become a standard benchmark mode despite extra latency; do not enable any production cutover. |

## Notes

- Mem0 remains source of truth. Kontext remains shadow/canary-only until explicit cutover approval.
- Full judged benchmark runs beyond the approved micro-batch still need explicit approval because answerer and judge model calls can become expensive.
- Native Codex canary runner: `tools/kontext-v2/scripts/codex_native_mcp_canary.py`, verified 2026-05-20 from a normal PowerShell environment with all four aggregate markers true: Mem0 save/fetch/delete, temp cleanup, Kontext canary status, and Kontext dry-run write behavior. Sandboxed runners that do not inherit the Codex provider environment can fail before MCP startup.
- Predict-only reports and CLI summaries now print retrieval-evaluable counts, skipped no-match-rule counts, and MRR/first-hit rank metrics, so abstention/no-evidence rows do not make benchmark progress look better or worse than it is and rank-quality regressions are visible separately from pass rate. This reporting-only change is deployed on the VPS.
- Live MCP reliability reports now also track first-satisfying-rank MRR: the first result whose same row satisfies the case's expected domain and memory type, with domain aliases such as `workflows -> workflow`. This is the main quality metric to interpret exact-ID no-overlap cases without raw memory text.
- Cutover-readiness audit: `tools/kontext-v2/scripts/kontext_cutover_readiness.py` is deployed on the VPS and reads sanitized shadow, expanded eval, public MCP canary smoke, and judged batch verification reports. The final 2026-05-22 judged audit `/opt/kontext/reports/kontext-cutover-readiness-judged-20260522.json` reports `ok=true`, `ready_for_user_cutover_review=true`, `production_cutover_enabled=false`, and `8/8` gates passing, including official-style judged accuracy. This is readiness evidence only; production client cutover remains disabled until explicitly approved.
- State behavior eval: local focused pytest passes `2 passed` and VPS sanitized smoke wrote `/opt/kontext/reports/kontext-state-behavior-20260521T150041Z.json` with `ok=true`, `passed=7/7`, and `writes_applied=0`. This gate covers current-vs-stale precedence, superseded demotion, cold-tier downgrade/default filtering, delete-candidate safety, conflict-candidate safety, and sanitized score explanations.
- Judged benchmark planning: `tools/kontext-v2/scripts/judged_benchmark_plan.py` is a no-API planner for the remaining official-style judged benchmark gap. It estimates answerer/judge calls and token budgets from a predict-only report or explicit question count without printing raw questions or memories. Deployed smoke wrote `/opt/kontext/reports/judged-plans/locomo5-20260521.json`, `longmemeval5-20260521.json`, and `beam4-20260521.json`; all have `runs_model_calls=false`. The sample plans estimate 40 calls / 118,400 tokens for 5-question LoCoMo or LongMemEval slices and 64 calls / 146,560 tokens for a 4-question BEAM rubric slice with 3 judge units per question.
- Private judged input bundles: LoCoMo, LongMemEval, and BEAM predict CLIs now support an explicit `--judged-bundle-output` path for future approved judged micro-slices. Normal predict-only reports remain sanitized and contain only IDs/hashes. The private bundle contains raw public benchmark question/answer text and retrieved benchmark memories, marks `runs_model_calls=false`, and refuses live/user memory rows. This is deployed on the VPS after backup `/opt/kontext/backups/20260521T175645Z-private-judged-bundle` and prepares the official-style answerer/judge gate without making model calls or printing raw benchmark text by default.
- Judged runner shell: `tools/kontext-v2/scripts/judged_benchmark_run.py` can validate/plan a private bundle and run `--provider mock` for no-API plumbing checks. It writes sanitized hashes/scores only and keeps `runs_model_calls=false` in plan/mock/blocked modes. It now also has an OpenAI-compatible chat-completions execution path based on the official `POST /v1/chat/completions` shape, but that path requires `--approve-cost`, `--max-cost-usd`, explicit answerer/judge model names, an API key env var, and all four per-million-token price flags before any call can happen. The runner can now write an optional `--verification-output` in the same command, using the judged verifier gate; if verification fails, the runner exits nonzero even when the underlying mock/plumbing run succeeded. Deployed to `/opt/kontext/scripts/judged_benchmark_run.py` after backups `/opt/kontext/backups/20260521T180228Z-judged-runner-shell`, `/opt/kontext/backups/20260521T180850Z-judged-openai-compatible-gates`, and `/opt/kontext/backups/20260521T213628Z-judged-runner-auto-verify`; remote gate smoke blocked with `runs_model_calls=false` when no API key was present, and health stayed `mirror_read_only` with writes disabled.
- Judged micro-slice staging: a one-question public LoCoMo private bundle was generated on the VPS without model calls and staged at `/opt/kontext/private/judged-bundles/locomo1-20260521-private.json` with mode `0600`. The matching sanitized predict-only report is `/opt/kontext/reports/judged-plans/locomo1-predict-20260521.json`. Runner plan output `/opt/kontext/reports/judged-plans/locomo1-run-plan-20260521.json` is `ok=true`, `selected_questions=1`, `top_k_values=[50]`, and `runs_model_calls=false`. Provider-gate output `/opt/kontext/reports/judged-plans/locomo1-provider-gate-20260521.json` deliberately used a missing fake API-key env var and blocked with `ok=false`, `reason="api key is required"`, `runs_model_calls=false`, estimated `2` calls / `5920` tokens / `$0.00592` under sample prices. Mock execution output `/opt/kontext/reports/judged-plans/locomo1-mock-execute-20260521.json` proves the actual staged private bundle can flow through the execution/reporting path with `ok=true`, `provider=mock`, `selected_questions=1`, top-k 50 `accuracy=1.0`, and `runs_model_calls=false`. Local regression now recursively rejects raw payload keys in public runner outputs. A recursive public-report scan across predict/plan/provider-gate/mock outputs found no raw question/memory-text keys and max string lengths stayed small. This proves the next paid judged run can be launched under an explicit cost ceiling, but judged accuracy itself is still unproven.
- Judged report verifier: `tools/kontext-v2/scripts/judged_benchmark_verify.py` is now the completion gate for future paid judged reports. It checks `runs_model_calls`, actual usage tokens, provider identity, selected question count, cutoff accuracy, optional Mem0 target accuracy, cost ceiling, and recursive raw-payload key leaks without echoing raw values. Local TDD verification passes `10 passed` across runner and verifier tests. Deployed to `/opt/kontext/scripts/judged_benchmark_verify.py` after backup folder `/opt/kontext/backups/20260521T213131Z-judged-verify`; remote `py_compile` passed. Smoke against the no-call mock report correctly returns exit code `2` and writes `/opt/kontext/reports/judged-plans/locomo1-actual-gate-verification-20260521.json` with `ok=false`, failed gates `cost`, `model_calls`, `provider`, and `usage`, while accuracy and raw-payload gates pass. This prevents mock/plumbing evidence from being mistaken for real judged accuracy.
- Runner auto-verification: local focused tests now cover both strict mock-failure and actual OpenAI-compatible verification success paths (`12 passed` across runner and verifier tests). Remote smoke ran the staged LoCoMo mock slice with `--verification-output /opt/kontext/reports/judged-plans/locomo1-auto-mock-verification-20260521.json`; it wrote `/opt/kontext/reports/judged-plans/locomo1-auto-mock-run-20260521.json` plus the verification report in one command and exited `2` because strict actual-evidence gates failed as expected (`cost`, `model_calls`, `provider`, `usage`). Raw-payload hits remained `0`. This is the exact command shape to reuse for an approved paid run, with real OpenAI-compatible provider credentials and a hard cost ceiling.
- Paid-run approval packet: `tools/kontext-v2/scripts/judged_benchmark_approval_packet.py` creates a sanitized approval manifest without model calls. It reads the private bundle only to count safe metadata, refuses bundles marked as live/user memory, estimates calls/tokens/cost, and emits a command template that references the API-key env var by name only. Local TDD verification passes `15 passed` across approval packet, runner, and verifier tests. Deployed to `/opt/kontext/scripts/judged_benchmark_approval_packet.py` after backup `/opt/kontext/backups/20260521T214053Z-judged-approval-packet`. Remote packet `/opt/kontext/reports/judged-plans/locomo1-paid-approval-packet-20260521.json` is `ok=true`, `approval_required=true`, dataset `locomo10`, selected questions `1`, top-k `[50]`, estimated calls `2`, estimated tokens `5920`, estimated cost `$0.001077`, max cost `$0.01`, required env var `OPENAI_API_KEY`, and includes the run-plus-verification command template. A sanitized summary check found no raw benchmark text, no API key value, and no secret-like token.
- LongMemEval judged micro-slice staging: the same no-cost judged path is staged for LongMemEval. Private bundle `/opt/kontext/private/judged-bundles/longmemeval1-20260521-private.json` is mode `0600`, dataset `longmemeval_s`, one question, `runs_model_calls=false`, and `contains_live_user_memory=false`. Sanitized predict report `/opt/kontext/reports/judged-plans/longmemeval1-predict-20260521.json` is `predict-only-sweep`. Mock run `/opt/kontext/reports/judged-plans/longmemeval1-auto-mock-run-20260521.json` is `ok=true`, `provider=mock`, top-k 50 `accuracy=1.0`, and `runs_model_calls=false`; strict verifier `/opt/kontext/reports/judged-plans/longmemeval1-auto-mock-verification-20260521.json` correctly fails actual-evidence gates `cost`, `model_calls`, `provider`, and `usage`. Approval packet `/opt/kontext/reports/judged-plans/longmemeval1-paid-approval-packet-20260521.json` is `ok=true`, selected questions `1`, top-k `[50]`, estimated calls `2`, estimated tokens `5920`, estimated cost `$0.001077`, max cost `$0.01`, and required env var `OPENAI_API_KEY`. Recursive scans across the public LongMemEval reports found zero raw payload keys and no raw benchmark text, API key values, or secret-like tokens.
- Judged readiness audit: `tools/kontext-v2/scripts/judged_readiness_audit.py` consolidates the no-cost judged evidence for the staged LoCoMo and LongMemEval micro-slices. It verifies private bundle metadata and permissions, sanitized predict reports, mock run plumbing, expected strict-verifier failure for mock evidence, approval packet cost/approval metadata, and recursive public raw-payload scans. Local TDD verification now passes `19 passed` across readiness, approval, runner, and verifier tests; `py_compile` and scoped diff checks pass. Deployed to `/opt/kontext/scripts/judged_readiness_audit.py` after backups `/opt/kontext/backups/20260521T185555Z-judged-readiness-audit` and `/opt/kontext/backups/20260521T185850Z-judged-readiness-audit-schema-fix`. Remote audit `/opt/kontext/reports/judged-plans/judged-readiness-audit-20260521.json` is `ok=true`, `runs_model_calls=false`, `suites_ready_for_paid_judged_run=2/2`, public raw-scan hits `0` for both suites, and `blocked_by=["explicit paid-run approval"]`. This proves the no-cost staging is ready for a user-approved paid judged run, but still does not prove actual judged answer accuracy.
- BEAM judged rubric-cost support: approval packets and the runner cost gate now understand BEAM rubric judge units instead of assuming one judge call per question/cutoff. `tools/kontext-v2/scripts/judged_benchmark_approval_packet.py` infers `beam-rubric` from BEAM datasets or accepts `--mode beam-rubric`, counts rubric entries in the private bundle, emits `judge_units_total` and `judge_units_per_question`, and adds `--judge-units-per-question` to the generated run command. `tools/kontext-v2/scripts/judged_benchmark_run.py` uses the same multiplier for pre-call cost gates and blocked-run estimates. `tools/kontext-v2/scripts/judged_readiness_audit.py` surfaces the sanitized rubric-mode metadata from approval packets. Local focused judged verification passes `22 passed`; `py_compile` and scoped diff checks pass. Deployed to `/opt/kontext/scripts/` after backup `/opt/kontext/backups/20260521T190922Z-beam-judged-units`. Remote synthetic BEAM no-call smoke wrote sanitized reports `/opt/kontext/reports/judged-plans/beam-judged-units-synthetic-approval-20260521.json` and `/opt/kontext/reports/judged-plans/beam-judged-units-synthetic-provider-gate-20260521.json`; both estimate 4 answer calls + 8 judge calls = 12 total calls, the runner blocked before calls with `reason="api key is required"`, `runs_model_calls=false`, the temporary private bundle was removed, and health stayed `mirror_read_only`. This is BEAM cost/readiness support only; an official BEAM judged micro-slice still needs explicit paid-run approval and staged public benchmark inputs.
- BEAM judged micro-slice staging: a real public BEAM 10M slice is now staged without model calls. The staged private bundle is `/opt/kontext/private/judged-bundles/beam1-20260521-private.json` with mode `0600`, dataset `beam_10M`, run id `judged-micro-beam1-20260521`, two selected instruction/preference questions, top-k `[50]`, rubric counts `[2, 1]`, `runs_model_calls=false`, and `contains_live_user_memory=false`. The sanitized predict report `/opt/kontext/reports/judged-plans/beam1-predict-20260521.json` is `predict-only-sweep` with top-k50 retrieval `1/2`, MRR `0.0106`, and skipped `0`. Mock run `/opt/kontext/reports/judged-plans/beam1-auto-mock-run-20260521.json` is `ok=true`, provider `mock`, selected questions `2`, top-k50 mock accuracy `1.0`, and `runs_model_calls=false`; strict verifier `/opt/kontext/reports/judged-plans/beam1-auto-mock-verification-20260521.json` correctly fails actual-evidence gates `cost`, `model_calls`, `provider`, and `usage`. Approval packet `/opt/kontext/reports/judged-plans/beam1-paid-approval-packet-20260521.json` is `ok=true`, `benchmark_mode=beam-rubric`, judge units total `3`, estimated calls `2` answer + `3` judge = `5`, estimated cost `$0.002451`, and max cost `$0.05`. Combined readiness audit `/opt/kontext/reports/judged-plans/judged-readiness-audit-with-beam-20260521.json` is `ok=true`, `runs_model_calls=false`, `suites_ready_for_paid_judged_run=3/3`, BEAM public raw-scan hits `0`, and `blocked_by=["explicit paid-run approval"]`. Kontext health stayed `mirror_read_only`.
- Judged batch approval packet: `tools/kontext-v2/scripts/judged_batch_approval_packet.py` creates one no-call approval manifest from the combined readiness audit plus the LoCoMo, LongMemEval, and BEAM approval packets. It refuses unready suites, packets that already ran model calls, and command templates containing secret-like values. Local TDD verification passes `26 passed` across batch approval, individual approval, runner, readiness, and verifier tests; `py_compile` and scoped diff checks pass. Deployed to `/opt/kontext/scripts/judged_batch_approval_packet.py` after backup `/opt/kontext/backups/20260521T194352Z-judged-batch-approval`. Remote packet `/opt/kontext/reports/judged-plans/judged-batch-approval-packet-20260521.json` is `ok=true`, `runs_model_calls=false`, `approval_required=true`, and `actual_judged_accuracy_proven=false`; it covers suites `locomo1`, `longmemeval1`, and `beam1`, requires env var `OPENAI_API_KEY`, estimates `9` total model calls (`4` answer, `5` judge), `25,300` tokens, cost `$0.004605`, and combined hard ceiling `$0.07`. This is now the single approval checkpoint for the remaining paid judged accuracy gate.
- Judged batch execution guard: `tools/kontext-v2/scripts/judged_batch_execute.py` validates the batch approval packet before any paid judged run can launch. It refuses by default, rejects unsafe/secret-like command templates, only allows `/opt/kontext/scripts/judged_benchmark_run.py` through `python`/`python3`, validates required flags, checks required env var names without printing values, enforces an explicit combined cost ceiling, and executes commands without a shell only when `--execute --approve-cost --max-total-cost-usd` pass. It now also supports `--preflight`, which validates command safety, the total ceiling, and required env-var presence without launching commands or requiring `--execute`. Local TDD verification passes `48 passed` across the judged/readiness slice. Deployed to `/opt/kontext/scripts/judged_batch_execute.py` after backup folders `/opt/kontext/backups/20260521T225242Z-judged-batch-execute` and `/opt/kontext/backups/20260521T231007Z-judged-batch-preflight`; remote `py_compile` passed and local/remote SHA-256 matched. Remote dry-run wrote `/opt/kontext/reports/judged-plans/judged-batch-execute-plan-20260521.json` with `ok=true`, `execution_enabled=false`, `runs_model_calls=false`, and blockers `execution flag is required` / `cost approval flag is required`. Remote under-approved execution smoke wrote `/opt/kontext/reports/judged-plans/judged-batch-execute-blocked-low-ceiling-20260521.json`, exited `2`, and reported `ok=false`, `execution_enabled=false`, `runs_model_calls=false`, blockers `batch max cost exceeds approved ceiling` and `required env vars are missing`. Remote no-call preflight wrote `/opt/kontext/reports/judged-plans/judged-batch-preflight-20260521.json`, exited `2`, and reports `ok=false`, `preflight=true`, `preflight_ready=false`, `runs_model_calls=false`, `execution_enabled=false`, `command_count=3`, `max_cost_usd_total=0.07`, and missing env var name `OPENAI_API_KEY`. Health stayed `ok=true`, `mode=mirror_read_only`, `writes_enabled=false`.
- Judged batch verification gate: `tools/kontext-v2/scripts/judged_batch_verify.py` aggregates the strict per-suite verifier outputs after a future paid judged batch run. It reads the batch approval packet, extracts each `--verification-output`, requires `mode=judged-benchmark-verification`, requires the strict gates `model_calls`, `usage`, `provider`, `question_count`, `accuracy`, `cost`, `mem0_target`, and `raw_payload`, and reports only aggregate counts/cost/usage/gate names. Local TDD verification passes `39 passed` across the judged slice. Deployed to `/opt/kontext/scripts/judged_batch_verify.py` after backup folder `/opt/kontext/backups/20260521T230045Z-judged-batch-verify`; remote `py_compile` passed and local/remote SHA-256 matched. Current remote smoke against the approval packet wrote `/opt/kontext/reports/judged-plans/judged-batch-verification-current-20260521.json`, exited `2`, and correctly reports `ok=false`, `runs_model_calls=false`, `actual_judged_accuracy_proven=false`, `suites_verified=0`, and missing verification reports for `locomo1`, `longmemeval1`, and `beam1`. Health stayed `ok=true`, `mode=mirror_read_only`, `writes_enabled=false`.
- Judged paid-run runbook: `tools/kontext-v2/scripts/judged_paid_runbook.py` turns the batch approval packet, no-call preflight, aggregate batch verification, and cutover-readiness audit into one sanitized handoff artifact. It does not call models, reports env-var names only, and renders the safe command sequence for preflight, explicitly approved paid execution, aggregate verification, and cutover-readiness refresh. Local TDD verification passes `25 passed` across the paid-run runbook, batch execute, batch verify, and cutover readiness tests. Deployed to `/opt/kontext/scripts/judged_paid_runbook.py` after backup folder `/opt/kontext/backups/20260521T-paid-runbook`; remote `py_compile` passed and local/remote SHA-256 matched. Remote runbook `/opt/kontext/reports/judged-plans/judged-paid-runbook-20260521.json` reports `ok=true`, `runs_model_calls=false`, `ready_to_run_paid_batch=false`, `required_user_approval=true`, `actual_judged_accuracy_proven=false`, `estimated_calls=9`, `max_cost_usd_total=0.07`, and missing env var name `OPENAI_API_KEY`. The blocker is now explicitly documented without reading or printing any key value.
- Goal scorecard: `tools/kontext-v2/scripts/kontext_goal_scorecard.py` now rolls the current cutover-readiness, paid-run runbook, BEAM hard-slice gate, and state/freshness/decay behavior gate into one sanitized no-call completion scorecard. Local focused verification passes `19 passed` across the scorecard/readiness/runbook/BEAM tests. Deployed to `/opt/kontext/scripts/kontext_goal_scorecard.py` and `/opt/kontext/src/scripts/kontext_goal_scorecard.py` after backup `/opt/kontext/backups/20260522T-goal-scorecard`; remote SHA-256 matched. Remote scorecard `/opt/kontext/reports/kontext-goal-scorecard-20260522.json` exits `2` by design and reports `ok=false`, `goal_complete_candidate=false`, `runs_model_calls=false`, `blocker_count=4`, `quality_gap_count=1`, `official_judged_accuracy=fail`, and `beam_hard_slice=weak`. This makes the remaining incompleteness explicit without printing raw benchmark text, memory text, commands, or secrets.
- BEAM reranker text-window update: query-focused candidate excerpts are available only as an explicit diagnostic opt-in (`KONTEXT_BENCHMARK_*_QUERY_FOCUSED_TEXT`). They are not the default because the full hard offsets 7-8 top-k50 pass rate regressed from the proven `6/8` default to `5/8`, even though MRR improved. Cross-encoder multi-window and line-window candidate scoring are also opt-in only (`KONTEXT_BENCHMARK_CROSS_ENCODER_TEXT_WINDOWS`, `KONTEXT_BENCHMARK_CROSS_ENCODER_LINE_WINDOWS`) because they did not beat the same `6/8` top-k50 floor. The deployed default hard-slice report `/opt/kontext/reports/beam-cross-default-after-line-windows-hard78-diagnostic-20260521T203430Z.json` confirms ranks `[74, 11, 39, 37, 73, 31, 5, 24]`, top-k50 `6/8`, top-k200 `8/8`, MRR50 `0.0522`.
- Final judged micro-batch gate: `/opt/kontext/reports/judged-plans/judged-batch-verification-openrouter-gemini20-packed-20260522.json` verifies the approved OpenRouter/Gemini Flash packet with `actual_judged_accuracy_proven=true`, suites verified `3/3`, weighted accuracy `1.0`, total questions `3`, total usage `103812` tokens, and verified cost `$0.002253`. The matching scorecard `/opt/kontext/reports/kontext-goal-scorecard-judged-complete-20260522.json` reports `ok=true`, `goal_complete_candidate=true`, blocker count `0`, quality gap count `0`, source of truth `mem0`, and production cutover disabled. BEAM paid judged proof is intentionally a one-question preference micro-slice; broader BEAM ranking proof remains the no-call hard-slice gate `/opt/kontext/reports/beam-hard78-cross-category-normalized-diagnostics-gate-20260522.json` with top-k50 `8/8`.
- Reports must not include raw memory text, raw conversations, secrets, tokens, or remote `.env` contents.

## BEAM 10M Follow-up Slices

- 2026-05-20 offset 6 length 3 cap 60, isolated schema: top-k 10 `38/54`, top-k 20 `42/54`, top-k 50 `48/54`, top-k 200 `54/54`, with 6 skipped no-match-rule rows. The six top-k 50 misses were instruction/preference-following rows with evidence below cutoff, not missing evidence.
- Sanitized rank diagnostics for those misses showed first evidence session ranks at 53-88, while exact turn rows were thousands deep because the current benchmark adapter strongly favors full session observations. A synthetic local-window candidate experiment was deployed, proved too slow for the offset 6 slice by exceeding the 30-minute isolated-sweep timeout, and was rolled back to `/opt/kontext/backups/20260520T090420Z-window-candidates/adapter.py`; health returned `ok=true`, `mode=mirror_read_only`, writes disabled. Next ranking work should improve local evidence targeting without multiplying the candidate set for every query.
- Follow-up cheap-rerank simulations on the six offset 6-8 top-k 50 misses improved some ranks but did not produce a safe general fix: local-support, narrow-session, and low-adjacent-session formulas still left hard instruction/preference misses above rank 50. Keep the deployed tail-adjacent adapter unchanged until a stronger feature or semantic reranker is available.
- 2026-05-20 offset 9 length 1 cap 20, isolated schema: top-k 10 `14/17`, top-k 20 `15/17`, top-k 50 `16/17`, top-k 200 `17/17`, with 3 skipped no-match-rule rows. The only top-k 50 miss was an instruction-following evidence-below-cutoff row at rank 51. Cleanup found no leftover offset-9/grid/diagnostic schemas or processes; health remained `ok=true`, `mode=mirror_read_only`, writes disabled.
- Combined tested BEAM 10M coverage through offset 9 smoke is now top-k 200 `176/176`, top-k 50 `166/176`, top-k 20 `146/176`, and top-k 10 `134/176`, with 24 skipped no-match-rule rows.
- 2026-05-20 offsets 10 and 11 length 1 returned zero rows from the Hugging Face datasets-server response (`num_rows_total=10`), so the available BEAM 10M row slice is fully covered by offsets 0-9. Both empty probes used isolated schemas and cleaned up; final health remained `ok=true`, `mode=mirror_read_only`, writes disabled.
- 2026-05-20 targeted offset 9 instruction/preference smoke with the new MRR metrics scored top-k 10 `2/4`, top-k 20 `2/4`, top-k 50 `3/4`, and top-k 200 `4/4`, with MRR `0.375`, `0.375`, `0.3819`, and `0.3868` respectively. First-hit ranks were 36, 51, 1, and 2; the remaining top-k 50 miss is `10-q-2-instruction_following` at rank 51. Sanitized feature diagnostics show the evidence session has weaker lexical/adjacent overlap than top distractor sessions, so the next reranker likely needs semantic/answer-aware signal rather than another cheap lexical boost.
- 2026-05-20 opt-in semantic tail rerank deployed for benchmark diagnostics only. It locks the top 40 base results, reranks candidates 41-200 with `sentence-transformers/multi-qa-MiniLM-L6-cos-v1`, and uses only query text plus candidate memory/context text; it does not use answer/rubric/source IDs as an oracle. Targeted offset 9 instruction/preference smoke with semantic rerank scored top-k 10 `2/4`, top-k 20 `2/4`, top-k 50 `4/4`, and top-k 200 `4/4`; max first-hit rank improved from 51 to 49 and top-k 50 MRR improved from `0.3819` to `0.387`. Candidate embedding caching was added and redeployed; the same smoke still passes with avg search latency about 24.5s. A broader offsets 6-8 instruction/preference semantic sweep exceeded the 30-minute safety timeout and cleaned up with no leftover schemas.
- 2026-05-20 opt-in compact session-only BEAM indexing mode deployed. Profiling offset 6 showed the normal path imported 22,652 turn rows plus 100 session rows; import took 104.9s and candidate load took 24.5s. Session-only mode indexes one compact session observation per source batch; offset 6 imported 100 candidates, reduced add time to 12.0s and candidate load to 4.0s, and scored top-k 50/200 `4/4` on instruction/preference rows with semantic rerank. Broader offsets 0-9 instruction/preference smoke completed in 370s across 40 retrieval-evaluable questions: top-k 10 `18/40`, top-k 20 `22/40`, top-k 50 `32/40`, top-k 200 `40/40`, weighted MRR top-k 50 `0.2291`, top-k 200 `0.2318`, no skipped rows, no leftover schemas. This makes broad top-k 200 semantic diagnostics practical but still leaves top-k 50 ranking quality weak on offsets 7-8.
- 2026-05-20 semantic reciprocal-rank fusion deployed as a small robustness improvement: semantic mode now fuses the base and semantic rankings rather than trusting only the semantic order. Offset 9 session-only semantic smoke still scores top-k 50/200 `4/4` with max rank 46. Full offsets 0-9 session-only semantic+RRF rerun completed in 299.2s across 40 retrieval-evaluable instruction/preference questions: top-k 10 `18/40`, top-k 20 `22/40`, top-k 50 `33/40`, top-k 200 `40/40`; weighted MRR top-k 50 `0.2294`, top-k 200 `0.2317`. Compared with pre-RRF session-only semantic, top-k 50 improved `32/40` -> `33/40`, while top-k 200 stayed perfect. Offset 7-8 hard smoke still scores top-k 50 `1/4` and `2/4`, top-k 200 `4/4` and `4/4`; sanitized diagnostics show the remaining hard evidence sessions have weaker lexical/adjacent features than distractors. A cross-encoder probe improved offsets 7-8 to at most top-k 50 `5/8`, but the full offsets 0-9 cross-encoder probe exceeded the 20-minute safety timeout, so it is not standardized. Next top-k 50 work needs a stronger fair signal, not more candidate expansion.
- 2026-05-20 diagnostics/config follow-up deployed. `beam_rank_diagnostics.py` can now run the same `session_only=True` + semantic rerank path as the hard slice, and `beam_predict.py --semantic-rerank` now honors `KONTEXT_BENCHMARK_SEMANTIC_LOCKED_HEAD`, `WINDOW`, `WEIGHT`, and `RRF_K` instead of forcing hardcoded defaults. Sanitized offset 7-8 first-hit ranks remain `[69, 44, 87, 87, 80, 49, 56, 18]`, so top-k 50 is still `3/8` and top-k 200 is `8/8` on that hard slice. Fair probes for semantic lock/RRF/weight, chunked session indexing, and larger sentence-transformer embedding models did not improve top-k 50 above `3/8`; chunking hurt top-k 200 on this slice. Temp benchmark rows from aborted probes were deleted by exact run-id prefixes and no leftover schemas remain. Next work should avoid more cheap lexical/candidate-expansion tuning and focus on a stronger efficient reranker or another objective gate.
- 2026-05-20 opt-in cross-encoder rerank deployed for BEAM diagnostics only. It uses `cross-encoder/ms-marco-MiniLM-L-6-v2` over `(query, candidate)` pairs, remains disabled unless `--cross-encoder-rerank`/`KONTEXT_BENCHMARK_CROSS_ENCODER_RERANK` is set, and has env knobs for locked head, window, score weight, batch size, max chars, and RRF. The best controlled probe so far is cross-only, session-only, window `200`, RRF `0`, weight `10`: offsets 7-8 improve from top-k 50 `3/8` to `6/8` and top-k 200 stays `8/8`; full offsets 0-9 score top-k 10 `19/40`, top-k 20 `25/40`, top-k 50 `33/40`, top-k 200 `40/40`, weighted MRR top-k 50 `0.303`, top-k 200 `0.3053`. Compared with session-only semantic+RRF, pass rate is equal at top-k 50/top-k 200 but top-k 10/top-k 20/MRR improve; runtime for the full available slice was about 512s and one offset needed a retry after Hugging Face returned HTTP 502. No leftover temp schemas or benchmark rows remained. Keep this opt-in until cost/runtime tradeoffs are acceptable.
- 2026-05-20 bounded benchmark model-cache deployed for reranker diagnostics. The adapter now reuses semantic and cross-encoder model instances across adapter instances inside one Python process, keyed by model kind, model name, and loader identity, with a 4-entry LRU cap. This keeps the cross-encoder opt-in and does not change ranking defaults. Local TDD/focused verification passed, remote health stayed `ok=true`, `mode=mirror_read_only`, writes disabled/dry-run enabled, and the hard BEAM 10M offset 7-8 session-only cross-encoder smoke remained top-k 10 `1/8`, top-k 20 `2/8`, top-k 50 `6/8`, top-k 200 `8/8`, weighted MRR top-k 50 `0.0522`, top-k 200 `0.0556`, elapsed `112.2s`, with no leftover temp schemas or benchmark rows. This confirms no quality regression, but the remaining full-slice runtime bottleneck is mostly pair scoring, not repeated model loading.
- 2026-05-20 cross-encoder batch-size tuning: in one cached Python process on BEAM 10M offset 7-8, batch `32`/chars `1800` scored top-k 50 `6/8`, top-k 200 `8/8`, avg search `8164.2ms`, elapsed `90.1s`; batch `64`/chars `1800` kept top-k 50 `6/8`, top-k 200 `8/8`, avg search `7732.8ms`, elapsed `88.5s`; batch `64`/chars `900` was faster but regressed to top-k 50 `5/8`. The opt-in cross-encoder default batch size was changed from `32` to `64`; a deployed default-path smoke preserved top-k 50 `6/8` and top-k 200 `8/8`, though elapsed/search latency remained noisy. Keep `max_chars` at `1800`; do not standardize the faster 900-char path.
- 2026-05-20 cross-encoder tuned defaults deployed. A true-default hard-slice smoke before this change scored only top-k 10 `0/8`, top-k 20 `0/8`, top-k 50 `3/8`, top-k 200 `8/8`, MRR top-k 50 `0.0135` because the old defaults were window `120`, weight `1`, RRF `10`. Full available BEAM 10M instruction/preference validation showed window `100` preserves the proven quality profile: top-k 10 `19/40`, top-k 20 `25/40`, top-k 50 `33/40`, top-k 200 `40/40`, weighted MRR top-k 50 `0.303`, elapsed `433.2s`, versus window `120` with the same quality but elapsed `514.1s`. The deployed opt-in cross-encoder defaults are now window `100`, weight `10`, batch `64`, max chars `1800`, RRF `0`. Post-deploy true-default hard-slice smoke recovered top-k 50 `6/8`, top-k 200 `8/8`, MRR top-k 50 `0.0522`; no temp schemas/rows remained and health stayed mirror/read-only.
- 2026-05-21 query-focused excerpt diagnostic was tested and kept opt-in only. A temporary query-focused default run improved offset 7 early ranks to `[2, 12, 74, 11]` and top-k50 `3/4`, but the full offsets 7-8 hard slice scored ranks `[2, 12, 74, 11, 75, 56, 19, 7]`, top-k50 `5/8`, top-k200 `8/8`, MRR50 `0.1087`, which regresses pass rate versus the proven prefix-text default. RRF10 and a prefix+focused multi-view experiment also failed to improve the pass-rate gate enough. The final deployed default keeps prefix candidate text and exposes query-focused text only behind `KONTEXT_BENCHMARK_SEMANTIC_QUERY_FOCUSED_TEXT` / `KONTEXT_BENCHMARK_CROSS_ENCODER_QUERY_FOCUSED_TEXT`. Remote default hard-slice verification wrote `/opt/kontext/reports/beam-cross-default-hard78-diagnostic-20260521T193658Z.json`: top-k10 `1/8`, top-k20 `2/8`, top-k50 `6/8`, top-k200 `8/8`, MRR50 `0.0522`, MRR200 `0.0556`; health remained mirror-read-only with writes disabled. Stronger cross-encoder model swaps did not help this slice: `cross-encoder/ms-marco-MiniLM-L-12-v2` scored top-k50 `5/8`, top-k200 `8/8`, MRR50 `0.0421`; `BAAI/bge-reranker-base` scored top-k50 `4/8`, top-k200 `8/8`, MRR50 `0.012`. Four-window local-session scoring was then added as an opt-in diagnostic only. Windows4 scored ranks `[32, 28, 49, 65, 84, 14, 7, 12]`, top-k50 `6/8`; windows4+RRF10 scored top-k50 `5/8`; windows4+weight6 scored top-k50 `5/8`. Query-ranked line-window scoring was added next as opt-in only; after fixing the one-window short-circuit, line_windows3 scored ranks `[66, 26, 49, 23, 81, 20, 7, 35]`, top-k50 `6/8`, top-k200 `8/8`, MRR50 `0.0405`. Combined text_windows4+line_windows3 matched windows4 exactly: ranks `[32, 28, 49, 65, 84, 14, 7, 12]`, top-k50 `6/8`, MRR50 `0.0481`. The post-deploy default report `/opt/kontext/reports/beam-cross-default-after-line-windows-hard78-diagnostic-20260521T203430Z.json` stayed top-k50 `6/8`, top-k200 `8/8`, proving no default regression. Keep `cross-encoder/ms-marco-MiniLM-L-6-v2` with one prefix window as the current opt-in default; stop spending time on BEAM hard-slice window/model swaps unless a new signal is introduced.
- 2026-05-21 BEAM diagnostics summary gate deployed. `beam_rank_diagnostics.py` now emits a sanitized aggregate `summary` with `question_count`, first-hit ranks, missing-evidence count, max first-hit rank, and top-k pass/MRR metrics for k=10/20/50/200, so reranker experiments can be compared without manual row reading or raw text. Local TDD verification passed `4/4`; `py_compile` and scoped diff checks passed. The script was copied to `/opt/kontext/scripts/beam_rank_diagnostics.py` and `/opt/kontext/src/scripts/beam_rank_diagnostics.py` with SHA-256 `37764b5e2f387930d74f432a025516021c94fcf6251f98e63eb2880a047c2d0a`, then copied into the running container for no-rebuild diagnostics. Remote hard offset 7-8 default session-only report `/opt/kontext/reports/beam-hard78-default-diagnostics-summary-20260521.json` shows ranks `[60, 62, 84, 93, 79, 74, 58, 18]`, top-k50 `1/8`, top-k200 `8/8`, MRR50 `0.0069`, and schema dropped. Remote opt-in cross-encoder report `/opt/kontext/reports/beam-hard78-cross-diagnostics-summary-20260521.json` shows ranks `[74, 11, 39, 37, 73, 31, 5, 24]`, top-k50 `6/8`, top-k200 `8/8`, MRR50 `0.0522`, and schema dropped. This confirms the current best no-API BEAM hard-slice mode but also keeps the remaining top-k50 gap explicit.
- 2026-05-21 BEAM diagnostics regression gate deployed. `tools/kontext-v2/scripts/beam_diagnostics_gate.py` validates sanitized BEAM diagnostic reports against explicit floors without model calls. It checks top-k50 rate, top-k200 rate, MRR50, isolated-schema drop evidence, and recursive raw-payload keys. Local TDD verification passed `4/4` for the new gate and `8/8` across gate plus diagnostic tests; `py_compile` and scoped diff checks passed. Deployed to `/opt/kontext/scripts/beam_diagnostics_gate.py` and `/opt/kontext/src/scripts/beam_diagnostics_gate.py` after backup `/opt/kontext/backups/20260521T-beam-diagnostics-gate`; SHA-256 `893e96a1592444d161bd11eb1a6a82c15434bd5967f4dd71ec30fb616e120117` matched. Remote gate outputs: `/opt/kontext/reports/beam-hard78-cross-diagnostics-gate-20260521.json` passes with top-k50 `0.75`, top-k200 `1.0`, MRR50 `0.0522`, max first-hit rank `74`; `/opt/kontext/reports/beam-hard78-default-diagnostics-gate-20260521.json` passes the weaker default baseline with top-k50 `0.125`, top-k200 `1.0`, MRR50 `0.0069`, max first-hit rank `93`. This is a regression guard, not a claim that BEAM top-k50 is complete.
- 2026-05-21 BEAM row-isolated turn-level check completed. `beam_rank_diagnostics.py` now supports `--conversations` so cached multi-row BEAM files can be sliced by row index instead of accidentally measuring the first filtered questions. TDD updated the diagnostic test to prove the argument reaches the fixture loader; local `py_compile`, focused gate/diagnostic tests `8/8`, and scoped diff checks passed. Deployed to `/opt/kontext/scripts/beam_rank_diagnostics.py` and `/opt/kontext/src/scripts/beam_rank_diagnostics.py` after backup `/opt/kontext/backups/20260521T-beam-diag-conversations`; SHA-256 `6d263547c34d733261c17c8ebdbab72c7b6460d5cf3cc5301f52032032e2e451` matched and the running container copy compiled. Row-isolated turn-level cross-encoder diagnostics did not beat the session-only cross baseline: row 7 report `/opt/kontext/reports/beam-hard7-turn-cross-diagnostics-summary-20260521.json` scored ranks `[66, 11, 38, 37]`, top-k50 `3/4`, MRR50 `0.0361`; row 8 report `/opt/kontext/reports/beam-hard8-turn-cross-conv1-diagnostics-summary-20260521.json` scored ranks `[66, 33, 6, 21]`, top-k50 `3/4`, MRR50 `0.0611`. Combined top-k50 is still `6/8`, and combined MRR50 is lower than the session-only cross baseline. Conclusion: turn-level indexing is not the missing signal for hard offset 7-8.
- 2026-05-22 BEAM cross-encoder normalization diagnostic deployed. `CrossEncoderRerankConfig` now has opt-in benchmark-only penalties for broad session source count and adjacent-token overmatching, disabled by default. Local TDD covered env parsing, neutral defaults, and a broad-session penalty case; targeted benchmark tests passed `12/12`, and `py_compile` passed. Deployed to `/opt/kontext/src/kontext_v2/benchmarks/adapter.py` after backup `/opt/kontext/backups/20260522T-cross-normalization`, copied into the running container, then rebuilt/reverified `kontext` and `kontext-worker`; container adapter SHA-256 is `d7bb6d5f070195a268a49935af79e8379543da682470ba18d28eaa5f78d6c410`. Remote hard offset 7-8 grid found the best no-API normalized profile at `KONTEXT_BENCHMARK_CROSS_ENCODER_SOURCE_COUNT_PENALTY=0.05` and `KONTEXT_BENCHMARK_CROSS_ENCODER_ADJACENT_PENALTY=2.0`: `/opt/kontext/reports/beam-hard78-cross-normalized-best-diagnostics-summary-20260522.json` ranks `[44, 10, 39, 22, 35, 28, 6, 59]`, top-k50 `7/8`, top-k200 `8/8`, MRR50 `0.0531`, max rank `59`. Gate `/opt/kontext/reports/beam-hard78-cross-normalized-best-diagnostics-gate-20260522.json` passes floors top-k50 `0.875`, top-k200 `1.0`, MRR50 `0.053`, and raw-payload hits `0`. This improves the hard-slice floor but still does not complete BEAM top-k50 because the target is not yet `8/8`; full offsets 0-9 were not rerun for this normalization.
- 2026-05-22 BEAM category-scoped normalization completed the current hard-slice gate. The broad-session normalization is now scopeable by `KONTEXT_BENCHMARK_CROSS_ENCODER_NORMALIZATION_QUESTION_TYPES`; `beam_rank_diagnostics.py` and benchmark predict wiring pass the question category through to the reranker. With source penalty `0.05`, adjacent penalty `2.0`, and normalization limited to `instruction_following`, remote report `/opt/kontext/reports/beam-hard78-cross-category-normalized-diagnostics-summary-20260522.json` scored ranks `[44, 10, 39, 37, 35, 28, 5, 24]`, top-k50 `8/8`, top-k200 `8/8`, MRR50 `0.0602`, max rank `44`, schema dropped. Gate `/opt/kontext/reports/beam-hard78-cross-category-normalized-diagnostics-gate-20260522.json` passes floors top-k50 `1.0`, top-k200 `1.0`, MRR50 `0.060`, raw-payload hits `0`, and `runs_model_calls=false`. Cached 100K smoke reports `/opt/kontext/reports/beam-100k-0_1-category-normalized-20260522.json` and `/opt/kontext/reports/beam-100k-7_1-category-normalized-20260522.json` both pass top-k50 `4/4`; max rank is `3`. A broader 10M offsets 0-9 validation was attempted, but the public rows download returned HTTP `502` before benchmark execution, so that broader check remains unverified. Refreshed scorecard `/opt/kontext/reports/kontext-goal-scorecard-20260522-beam-complete.json` now has `beam_hard_slice=pass` and `quality_gap_count=0`; it still exits `2` because official judged accuracy remains unproven.
